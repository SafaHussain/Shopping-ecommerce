
<div class="container" style="text-align: middle" %>
<% if user_signed_in? %>
    <%= form_for @category, url: admin_categories_path do |form| %>

        <% if @category.errors.any? %>

          <h2><%= pluralize(@category.errors.count, "error") %> prohibited this article from being saved:</h2>
          <ul>
          <% @category.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
       <% end %>
          <p> <%= form.label :name %>
          <%= form.text_field :name %>
          </p>
    <p>
    <%= form.label :image %>
    <%= form.file_field :image %>
    </p>

        <%= form.submit 'Submit' %>
    <% end %>
   
<% else %>
    <h1>Not accessible!</h1>
<% end %>
</div>
