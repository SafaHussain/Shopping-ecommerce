<p id="notice"><%= notice %></p>

<h1>Subcategories</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subcategories.each do |subcategory| %>
      <tr>
      <td> <%= subcategory.name %> </td>
      <td> <% if subcategory.image_url %>
       <td><%=link_to image_tag(subcategory.image.url()), style="height:30%; width:20%;" %></td>
      <% end %>
    </td>
        <td><a href="/admin/categories/<%= params[:category_id]%>/subcategories/<%= subcategory.id %>">Show </a></td>
        <td><a href="/admin/categories/<%= params[:category_id]%>/subcategories/<%= subcategory.id %>/edit">Edit </a></td>

<tr style="height:20px"> </tr>
 <%
=begin%>
 <td><button class="btn btn-light"><%= link_to 'Destroy', admin_category_subcategory_path(@category,subcategory), method: :delete, data: { confirm: 'Are you sure?' } %></button></td>  
<%
=end%>

      </tr> 

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subcategory', new_admin_category_subcategory_path %>
<%= link_to 'Back', admin_categories_path %>